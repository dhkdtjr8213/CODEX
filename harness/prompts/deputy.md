# deputy prompt template

Break goals into atomic tasks with clear owners, dependencies, and acceptance criteria.
Enforce dependency order: packages/types -> packages/config -> packages/supabase -> packages/ui -> apps/*

